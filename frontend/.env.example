# Leave unset for local dev — Vite's dev server proxies /api to localhost:5001.
# In production (Vercel), set this to the deployed Render backend URL, e.g.:
# VITE_API_URL=https://nyayai-backend-iscw.onrender.com
VITE_API_URL=
